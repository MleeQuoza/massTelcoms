<h1>New Donation</h1>
<%= form_for (@donation) do |f| %>
    <div class="field">
      <%= f.hidden_field :user_id, :value => current_user.id %>
      <p><%= f.label :amount %>: <%= f.number_field(:amount) %></p>
    </div>
    <div class="field">
      <p><%= f.submit %></p>
    </div>
<% end %>